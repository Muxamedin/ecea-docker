Semple of commands:

build rh agent  mage

./build.sh \
-c=/tmp/build \
-t=agent \
-s=rh


build ubuntu agent  image
  
./build.sh
-c=/tmp/build \
-t=agent \
-s=ubuntu

build centos agent image

./build.sh \
 -c=/tmp/build \
 -t=agent \
 -s=centos

 rebuild agent image
without copyying files from  opt (if  /tmp/build/agent/ecloud.tar.gz already exists )
./build.sh \
 -c=/tmp/build \
 -t=agent \
 -s=centos \
-r 

#--------------------------------------------------
./old_build agent "10.0.0"  /path/to/folder/with/content 
./old_build cm    "10.0.0"  /path/to/folder/with/content 
./old_build emake "10.0.0"   /path/to/folder/with/content 

#--------------------------------------------------

